# AMC24 Long-CoT Generalization V1

Status: `AMC24_LONG_COT_GENERALIZATION_V1_BLOCKED`.

This package records the required dataset, protocol, prompt, parser, model, and GPU audits. No AMC24 model was loaded and no AMC24 generation, smoke, subset, or full formal run was started because the repository and accessible local refs do not contain a canonical AMC24 dataset or evaluation protocol.

The blocker is scientific rather than computational: creating or combining an AMC dataset without an established source, split, problem IDs, answer mapping, and Avg@8/Maj@8 definition would create non-canonical evidence.

See `dataset_audit.md`, `protocol_audit.md`, `parser_audit.md`, and `final_gate.json`.
