# Stop / Go Decision

STOP full AMC24 generation.

Gate 0 failed: dataset identity, split, problem IDs, ground-truth mapping, AMC-specific parser contract, and Avg@8/Maj@8 protocol are unresolved. The available idle GPUs are intentionally unused.

GO only after a canonical AMC24 dataset and protocol manifest is committed and reviewed. That manifest must define the benchmark source/revision, checksum, exact rows, answer representation, prompt, eight fixed response seeds, majority tie behavior, and parser tests.
