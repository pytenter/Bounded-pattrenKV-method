# Design

Request-local vectors are authoritative when present. The legacy scalar `total_tokens` remains the equal-length compatibility value and stores the max request length for ragged assembled caches.
