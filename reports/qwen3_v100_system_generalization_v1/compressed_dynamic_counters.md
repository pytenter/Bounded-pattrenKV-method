# Compressed Dynamic Counters

Compressed B1 run reached packed-history path with full historical FP16 K/V materialization counters at zero and fallback_count at zero. Semantic parity still failed, so these counters do not establish backend readiness.
