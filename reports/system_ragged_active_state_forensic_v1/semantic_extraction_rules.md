# Semantic Extraction Rules

Only request-local SEMANTIC_ACTIVE regions are hashed. SEMANTIC_PADDING and INACTIVE_CAPACITY_UNDEFINED are excluded from first-divergence classification; full physical hashes may be reported only as artifacts.
