# NSYS / NCU Availability

`nsys` and `ncu` were not available on this server during Phase S1.5 profiling, so no raw GPU traces were collected and no large trace artifacts were committed.

The committed GPU-oriented evidence for this phase is the CUDA-event component profile in `../component_breakdown.csv`.
