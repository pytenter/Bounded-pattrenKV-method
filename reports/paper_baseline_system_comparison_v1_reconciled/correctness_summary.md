# Correctness Summary

119 formal rows are valid. All valid rows preserve true batch, zero serial dispatch counters, zero fallback, and decode-only timing boundaries. 4 CUDA OOM rows are expected capacity-stop probes and are excluded from summary statistics.
