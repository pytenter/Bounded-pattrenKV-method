# Seed Protocol

## Response Count

`responses_per_problem = 8`.

## Seeds

No existing canonical eight-response seed list was found in the repository. This protocol preregisters:

```text
response_id 0 -> seed 42
response_id 1 -> seed 43
response_id 2 -> seed 44
response_id 3 -> seed 45
response_id 4 -> seed 46
response_id 5 -> seed 47
response_id 6 -> seed 48
response_id 7 -> seed 49
```

Every method uses the same `(problem_id, response_id, seed)` identity for paired comparisons.

The seed list must not be changed after AMC24-Text results are observed.
