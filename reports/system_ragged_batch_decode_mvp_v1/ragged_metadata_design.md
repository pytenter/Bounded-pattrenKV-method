# Ragged Metadata Design

The prototype metadata is stored in `ragged_metadata.json` and includes request ids, seq_lens, position_ids, total tokens, packed counts, centroid slots, page_indptr, page counts, and last-page valid tokens.
