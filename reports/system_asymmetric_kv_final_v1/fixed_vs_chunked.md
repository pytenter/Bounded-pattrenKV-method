# Fixed vs Chunked

Final best backend: `chunked_capacity`.

Latency is decided from profile-off TPOT first; memory slack is the tie breaker when latency is within 1%.

Classification: `ASYMMETRIC_KV_BOTH_SUPPORTED`.
