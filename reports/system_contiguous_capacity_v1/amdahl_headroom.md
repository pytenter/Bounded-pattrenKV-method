# Amdahl Headroom

- Based on S4 cache mutation share: `15.73%`.
- This is a profile-based approximation, not an actual speedup prediction.

- If cache mutation is 1.25x faster: `1.0325x` max E2E speedup
- If cache mutation is 1.5x faster: `1.0553x` max E2E speedup
- If cache mutation is 2x faster: `1.0854x` max E2E speedup
- If cache mutation is infinite faster: `1.1867x` max E2E speedup
