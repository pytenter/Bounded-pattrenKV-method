{
  "status": "NOT_RUN_NO_DIRECT_APPEND_IMPLEMENTED",
  "reason": "decode-only profile showed page_batch_pack is absent from the measured decode window; direct compressed page append would target prefill/boundary flush, not current decode=8 TPOT"
}
